FROM node:16-alpine3.18

WORKDIR /usr/src/app

COPY . .

RUN npm install && \
    npm install -g serve && \
    REACT_APP_BACKEND_URL=http://localhost:80/api && \
    npm run build && \
    adduser -S appuser && \
    rm -rf node_modules

USER appuser

CMD ["serve", "-s", "-l", "5000", "build"]
